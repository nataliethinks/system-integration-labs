# Module 06 – API Security with JWT & HTTPS

This module focuses on securing REST APIs using authentication,
authorization, and encrypted communication.

## Overview
A Flask-based API is protected using JWT authentication.
Role-based access control is implemented to restrict sensitive endpoints.
HTTPS is enabled using a self-signed certificate for secure communication.

## Features
User login and JWT token issuance
Protected API endpoints
Role-based access control (user vs admin)
HTTPS-enabled API

## Technologies
Python (Flask)
PyJWT
OpenSSL
Postman